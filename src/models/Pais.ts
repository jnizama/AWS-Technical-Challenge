export class Pais {
    id: string;
    nombre: string;
    codigo: string;

    constructor(id: string, nombre: string, codigo: string) {
        this.id = id;
        this.nombre = nombre;
        this.codigo = codigo;
    }
}
